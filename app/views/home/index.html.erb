<section class="header-home">
  <section class="nav-home">
    <a href="#">Browse by:</a>
    <a href="#food">Food</a>
    <a href="#shop">Shop</a>
    <a href="#culture">Culture</a>
    <a href="#sports">Sports</a>
  </section>
  <section class="nav-search">
    <a href="<%= search_path %>">Search <%= fa_icon'binoculars' %></a>
  </section>
</section>



<div class="divider">
  <p class="name" id="food" href="#">Latest in Food</p>
</div>

<% @posts.food.each do |post| %>
<section class="posts">
  <section class="post-info">
    <p class="post-cat"><%= post.category if post.category? %></p>
    <p><%= post.formatted_address if post.formatted_address? %> </p>
    <h5><span class="blue-go">G</span><span class="red">O</span><span class="yellow">O</span><span class="blue-go">G</span><span class="green">L</span><span class="red">E</span> Rating</h5>
      <p> <%= post.rating if post.rating? %> </p>
  </section>
  <section class="post">
    <section class="username">
     <%= link_to post.user.full_name, user_posts_path(post.user) %>
    </section>
    <section class="post-user show">
      <% if post.user.avatar? %>
        <%= image_tag post.user.avatar.url(:thumb) %>
      <% else %>
        <img src="<%= image_url('an-user.png') %>">
      <% end %>
    </section>
    <section class="post-meta">
      <%= link_to post.title, post_path(post.id)  %>
      <p><%= post.created_at.strftime('Posted on a %A, %b %d %Y') %>, from a trip to <%= post.country %></p>
    </section>
    <section class="post-picture">
      <% if post.picture? %>
        <%= image_tag post.picture.url(:main) %>
      <% else %>
        <img src="<%= image_url('balloon.png') %>">
      <% end %>
    </section>
  
    <section class="post-content">
      <%= markdownify post.content.truncate(200) %><%= link_to '    Read more...', post_path(post.id)  %>
    </section>
    <section class="post-social">
       
       <div class="social-likes">
          <% if post.likes.count == 0 %>
            <%= fa_icon'paper-plane-o' %> <a href="<%= post_path(post.id) %>">No Likes</a>
          <% else %>
            <%= fa_icon'paper-plane' %><a href="<%= post_path(post.id) %>"><%= post.likes.count %> Likes</a>
          <% end %>
      </div>
      
      <div class="social-edit">
      <% if user_signed_in? && post.user_id == current_user.id %>
        <%= fa_icon'edit' %> <%= link_to 'Edit', edit_post_path(post.id)  %>
      <% else %>
        <p>|</p>
      <% end %>
      </div>
      <div class="social-comments">
        <%= fa_icon'comment' %> <a href="<%= post_path(post.id) %>"> <%= post.comments.count %> Comments</a>
      </div>
    </section>
  </section>
</section>
<% end %>

<div class="divider">
  <p class="name" id="shop" href="#">Latest in Shop</p>
</div>

<% @posts.shop.each do |post| %>
<section class="posts">
  <section class="post-info">
    <p class="post-cat"><%= post.category if post.category? %></p>
    <p><%= post.formatted_address if post.formatted_address? %> </p>
    <h5><span class="blue-go">G</span><span class="red">O</span><span class="yellow">O</span><span class="blue-go">G</span><span class="green">L</span><span class="red">E</span> Rating</h5>
      <p> <%= post.rating if post.rating? %> </p>
  </section>
  <section class="post">
    <section class="username">
     <%= link_to post.user.full_name, user_posts_path(post.user) %>
    </section>
    <section class="post-user show">
      <% if post.user.avatar? %>
        <%= image_tag post.user.avatar.url(:thumb) %>
      <% else %>
        <img src="<%= image_url('an-user.png') %>">
      <% end %>
    </section>
    <section class="post-meta">
      <%= link_to post.title, post_path(post.id)  %>
      <p><%= post.created_at.strftime('Posted on a %A, %b %d %Y') %>, from a trip to <%= post.country %></p>
    </section>
    <section class="post-picture">
      <% if post.picture? %>
        <%= image_tag post.picture.url(:main) %>
      <% else %>
        <img src="<%= image_url('balloon.png') %>">
      <% end %>
    </section>
  
    <section class="post-content">
      <%= markdownify post.content.truncate(200) %><%= link_to '    Read more...', post_path(post.id)  %>
    </section>
    <section class="post-social">
       
       <div class="social-likes">
          <% if post.likes.count == 0 %>
            <%= fa_icon'paper-plane-o' %> <a href="<%= post_path(post.id) %>">No Likes</a>
          <% else %>
            <%= fa_icon'paper-plane' %><a href="<%= post_path(post.id) %>"><%= post.likes.count %> Likes</a>
          <% end %>
      </div>
      
      <div class="social-edit">
      <% if user_signed_in? && post.user_id == current_user.id %>
        <%= fa_icon'edit' %> <%= link_to 'Edit', edit_post_path(post.id)  %>
      <% else %>
        <p>|</p>
      <% end %>
      </div>
      <div class="social-comments">
        <%= fa_icon'comment' %> <a href="<%= post_path(post.id) %>"> <%= post.comments.count %> Comments</a>
      </div>
    </section>
  </section>
</section>
<% end %>

<div class="divider">
  <p class="name" id="culture" href="#">Latest in Culture</p>
</div>

<% @posts.culture.each do |post| %>
<section class="posts">
  <section class="post-info">
    <p class="post-cat"><%= post.category if post.category? %></p>
    <p><%= post.formatted_address if post.formatted_address? %> </p>
    <h5><span class="blue-go">G</span><span class="red">O</span><span class="yellow">O</span><span class="blue-go">G</span><span class="green">L</span><span class="red">E</span> Rating</h5>
      <p> <%= post.rating if post.rating? %> </p>
  </section>
  <section class="post">
    <section class="username">
     <%= link_to post.user.full_name, user_posts_path(post.user) %>
    </section>
    <section class="post-user show">
      <% if post.user.avatar? %>
        <%= image_tag post.user.avatar.url(:thumb) %>
      <% else %>
        <img src="<%= image_url('an-user.png') %>">
      <% end %>
    </section>
    <section class="post-meta">
      <%= link_to post.title, post_path(post.id)  %>
      <p><%= post.created_at.strftime('Posted on a %A, %b %d %Y') %>, from a trip to <%= post.country %></p>
    </section>
    <section class="post-picture">
      <% if post.picture? %>
        <%= image_tag post.picture.url(:main) %>
      <% else %>
        <img src="<%= image_url('balloon.png') %>">
      <% end %>
    </section>
  
    <section class="post-content">
      <%= markdownify post.content.truncate(200) %><%= link_to '    Read more...', post_path(post.id)  %>
    </section>
    <section class="post-social">
       
       <div class="social-likes">
          <% if post.likes.count == 0 %>
            <%= fa_icon'paper-plane-o' %> <a href="<%= post_path(post.id) %>">No Likes</a>
          <% else %>
            <%= fa_icon'paper-plane' %><a href="<%= post_path(post.id) %>"><%= post.likes.count %> Likes</a>
          <% end %>
      </div>
      
      <div class="social-edit">
      <% if user_signed_in? && post.user_id == current_user.id %>
        <%= fa_icon'edit' %> <%= link_to 'Edit', edit_post_path(post.id)  %>
      <% else %>
        <p>|</p>
      <% end %>
      </div>
      <div class="social-comments">
        <%= fa_icon'comment' %> <a href="<%= post_path(post.id) %>"> <%= post.comments.count %> Comments</a>
      </div>
    </section>
  </section>
</section>
<% end %>

<div class="divider">
  <p class="name" id="sports" href="#">Latest in Sports</p>
</div>

<% @posts.sports.each do |post| %>
<section class="posts">
  <section class="post-info">
    <p class="post-cat"><%= post.category if post.category? %></p>
    <p><%= post.formatted_address if post.formatted_address? %> </p>
    <h5><span class="blue-go">G</span><span class="red">O</span><span class="yellow">O</span><span class="blue-go">G</span><span class="green">L</span><span class="red">E</span> Rating</h5>
      <p> <%= post.rating if post.rating? %> </p>
  </section>
  <section class="post">
    <section class="username">
     <%= link_to post.user.full_name, user_posts_path(post.user) %>
    </section>
    <section class="post-user show">
      <% if post.user.avatar? %>
        <%= image_tag post.user.avatar.url(:thumb) %>
      <% else %>
        <img src="<%= image_url('an-user.png') %>">
      <% end %>
    </section>
    <section class="post-meta">
      <%= link_to post.title, post_path(post.id)  %>
      <p><%= post.created_at.strftime('Posted on a %A, %b %d %Y') %>, from a trip to <%= post.country %></p>
    </section>
    <section class="post-picture">
      <% if post.picture? %>
        <%= image_tag post.picture.url(:main) %>
      <% else %>
        <img src="<%= image_url('balloon.png') %>">
      <% end %>
    </section>
  
    <section class="post-content">
      <%= markdownify post.content.truncate(200) %><%= link_to '    Read more...', post_path(post.id)  %>
    </section>
    <section class="post-social">
       
       <div class="social-likes">
          <% if post.likes.count == 0 %>
            <%= fa_icon'paper-plane-o' %> <a href="<%= post_path(post.id) %>">No Likes</a>
          <% else %>
            <%= fa_icon'paper-plane' %><a href="<%= post_path(post.id) %>"><%= post.likes.count %> Likes</a>
          <% end %>
      </div>
      
      <div class="social-edit">
      <% if user_signed_in? && post.user_id == current_user.id %>
        <%= fa_icon'edit' %> <%= link_to 'Edit', edit_post_path(post.id)  %>
      <% else %>
        <p>|</p>
      <% end %>
      </div>
      <div class="social-comments">
        <%= fa_icon'comment' %> <a href="<%= post_path(post.id) %>"> <%= post.comments.count %> Comments</a>
      </div>
    </section>
  </section>
</section>
<% end %>






